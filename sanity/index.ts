import { projectSchema } from './schemas/project'
import { postSchema } from './schemas/post'
import { testimonialSchema, teamMemberSchema } from './schemas/testimonial'

export const schemaTypes = [
  projectSchema,
  postSchema,
  testimonialSchema,
  teamMemberSchema,
]
